---

interface Props{
  url: string,
  name: string,
  color: string
}

const {url,name,color} = Astro.props;
---

<a href={url} class={`${color} py-2 px-7 rounded-md font-bold flex justifu-center items-center`}>
    <slot />
    <span>{name}</span>
</a>